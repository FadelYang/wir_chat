import React from 'react';
import wirFullLogo from '/logo-full.png';

const MainFullLogo = ({ className }) => {
  return <img src={wirFullLogo} className={className}></img>
};

export default MainFullLogo;